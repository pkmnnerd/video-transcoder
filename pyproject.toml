[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[project]
name = "video-transcoder"
version = "0.1.0"
description = "Single-user self-hosted video transcoding web app"
requires-python = ">=3.12"